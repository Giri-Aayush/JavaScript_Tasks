let a = 1, b = 1;

let c = ++a; // 2 , first increment happens and then assignment happens
let d = b++; // 1 , first assignment happens and then increment happens

//the value of a after these lines is 2
//the value of b after these lines is 2